/// Tamanho compartilhado por PriorityBadge e StatusBadge — mesmos 2
/// tamanhos (`sm`/`md`) que o protótipo usa nos dois componentes
/// (`size?: 'sm' | 'md'` em PriorityBadgeProps/StatusBadgeProps).
enum BadgeSize { sm, md }
